name: React Native CI - Test, Report & SonarCloud

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test-and-analyze:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: Front-end/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: Front-end

      - name: Run Jest Coverage Tests
        run: npm run test:coverage
        working-directory: Front-end

      # --- SỬA CÁC ĐƯỜNG DẪN DƯỚI ĐÂY ---

      - name: Check coverage files
        run: |
          ls -la Front-end/coverage/
          cat Front-end/coverage/lcov.info | head -20

      - name: Upload HTML Test Report
        uses: actions/upload-artifact@v4
        with:
          name: test-report
          path: Front-end/coverage/lcov-report/
          retention-days: 7

      - name: Upload Coverage to Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: coverage-lcov
          path: Front-end/coverage/lcov.info
          retention-days: 7

      - name: SonarQube Scan
        uses: SonarSource/sonarqube-scan-action@v4
        with:
          projectBaseDir: Front-end
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
